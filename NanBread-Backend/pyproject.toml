[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "nanbread"
version = "0.1.0"

[tool.setuptools.packages.find]
include = ["core*", "notebooks*"]